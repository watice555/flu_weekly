# 实施记录

## 2026-08-31 03:54:35 Asia/Shanghai — 迁移到 Projects_local

- 写入者模型：未知（运行环境未暴露）。
- 设备：TianhaodeMacBook-Pro.local（macOS 26.6.2，arm64）。

### 阶段总结

- 将整个项目从 `~/Projects/flu_weekly` 移至 `~/Projects_local/flu_weekly`；未保留旧路径软链接，脚本源码、依赖声明、22 份 PDF 和下载状态均原样保留。
- 在新目录重建 Python 3.12 虚拟环境，使用迁移前冻结的全部 9 个包及其精确版本；安装使用预先下载到本机临时目录的 wheel，无依赖升级。
- 将既有 `~/Cron/Scripts/flu-weekly.sh` 中的项目路径及 `~/Library/LaunchAgents/com.wuth.flu-weekly.plist` 中的工作目录改为新位置；入口、日志、每周六 07:10 计划和其他配置不变。
- 迁移期间卸载空闲的 LaunchAgent，完成后重新加载；没有手动启动任务、访问疾控网站或执行真实下载/坚果云同步。
- 修正 README 中的 Windows 路径与运行说明，补充现有定时任务及当前 Git/站点状态。新增 `.gitignore`，排除周报/状态目录、虚拟环境、缓存、日志和私有配置。
- 保持本项目独立；没有合并美团仓库、修改其 GitHub 配置或发布新站点。

### 已执行验证

- 移动后逐项校验迁移前清单中的 670 个文件/符号链接（文件内容 SHA-256、权限或符号链接目标一致）。随后再验证源码、依赖声明和周报/状态共 25 个原始文件未变。
- 22 份项目 PDF 及状态文件完整；坚果云周报目录中的 137 份 PDF 与迁移前哈希集合一致。
- 新虚拟环境的全部包版本与迁移前一致；`pip check`、直接运行 `pip --version`、激活环境后导入 requests/bs4 均通过，Python 与 pip 均指向新路径。
- `main.py` 语法检查通过。临时目录与 mock HTTP 的离线验证通过：新周报解析/下载/双目录复制/状态保存、同一周报跳过重复下载、下载失败时保留旧状态；未写入真实业务目录。
- `bash -n` 和 `plutil -lint` 通过。LaunchAgent 配置除工作目录外完全一致，wrapper 除项目路径外完全一致；重新加载后为 `not running`、`runs = 0`，计划仍为周六 07:10。
- 项目、虚拟环境、Cron 脚本和 LaunchAgent 的非缓存文件中没有活动旧路径引用；旧项目目录已不存在。
- 美团和日报仓库工作区保持干净、提交未变（分别为 `6e14aaa` 和 `02d1a13`）；日报已有 4 个未推送提交未被推送。

### 未执行与原因

- 未执行真实下载或外部同步：此次只要求迁移，使用离线 fixture 验证。
- 未初始化 Git、创建提交或配置远程：项目迁移前并非 Git 仓库，用户的 AGENTS.md 要求初始化前先询问；“继续”未被解读为对尚未询问的 Git 初始化授权。待用户确认后再创建独立仓库，或按后续决定并入既有仓库。
- 未创建或部署 GitHub Pages：用户仅提出后续考虑，当前代码只负责 PDF 下载和本地归档，站点/指标提取范围尚未确定。

## 2026-08-31 04:12:22 Asia/Shanghai — 建立版本管理并归集历史报告

- 写入者模型：未知（运行环境未暴露）。
- 设备：TianhaodeMacBook-Pro.local（macOS 26.6.2，arm64）。
- 阶段总结：用户明确授权后初始化 `main` 分支；从坚果云周报目录复制缺少的 115 份 PDF，22 份已有文件保持不变，共归集 137 份。扩展忽略规则，数据库、PDF、导出数据及临时校验文件均不提交。
- 验证：复制前先检查同名文件无内容冲突，复制后逐份 SHA-256 校验与源文件一致；未删除或修改坚果云文件。137 份 PDF 的前七页均可提取文本，南北方当周 ILI% 在各自报告内的重复表述一致。
- 边界：本阶段未创建远程仓库或发布网页；后续将保留当期数值及来源，历史同期引用不直接覆盖当期观测。

## 2026-08-31 04:39:31 Asia/Shanghai — 建立 ILI 数据库与南北方季节对比页面

- 写入者模型：未知（运行环境未暴露）。
- 设备：TianhaodeMacBook-Pro.local（macOS 26.6.2，arm64）。

### 阶段总结

- 新增 `flu_data`：从 PDF 正文抽取当周 ILI%，核验日期/周次、期号、百分比范围与重复表述。SQLite 保留 PDF SHA-256、版本、页码、原文、官方链接和异常说明；导出独立静态 JSON。未从图像估读，未把历史同期引用当作当期观测。
- 从官网解析并保存 138 期详情/PDF 链接。归档校验发现 `25-02.pdf` 与第 1 周完全相同，保留错存原件并从官网补正；同时补齐缺失的 `26-26.pdf`。坚果云的 137 份原件未改动。
- 对 2025 年第 52 周一处日期笔误，选择摘要中有原文支持、与周次一致的七天区间；对 2026 年第 11 周封面残留旧期号，选择含当周数值正文页面的一致期号。均保存异常说明，不静默修订原 PDF。
- 当前数据库含 138 期、276 条观测，日期区间 2024-01-01 至 2026-08-23，覆盖 2024-W01 至 2026-W34，无内部缺期，所有期次均有官方详情与 PDF 链接。
- 新增静态页面：南北方各一张图，第 14 周至次年第 13 周对齐，支持第 53 周、年度选择、数值提示、来源链接、年度表格与 CSV 导出。2023-2024 年度仅收录后段，明确标注；缺失周断线，不插值。以当期发布值绘图及计算周变化，不混用后续修订引用。
- 既有 `main.py` 下载后自动重建数据库与网页 JSON，再同步到坚果云并记录成功状态；无新 PDF 时仍可更新索引。下载中断或非 PDF 响应保留旧文件，修订文件另存原件；构建失败不推进成功状态、不把未验证的新文件同步到坚果云。
- 新增站点文件白名单打包工具，仅生成 HTML/CSS/JS/JSON 发布目录，不上传或部署。PDF、数据库、原文证据、缓存和运行数据均不进 Git。补充 README 和项目工作约定。

### 已执行验证

- 20 项 Python 单元测试及 4 项 JavaScript 测试全部通过：提取/冲突/周次与日期边界、来源解析、幂等导入、版本保留、整批失败回滚、失败下载保护、原件保留、数据库失败阻止同步与状态推进、发布文件边界、季节断点和第 53 周。
- 完整 PDF 归档独立重新提取到临时数据库，与正式导出的全部 138 期数值、日期、链接、异常说明完全一致；增量重建 `changed=0`。SQLite integrity_check/foreign_key_check 通过。
- Python compileall、JavaScript 语法检查、pip check、git diff --check 通过。
- 137 份坚果云 PDF 与迁移前 SHA-256 清单一致，项目内对应副本/保留的错存原件逐项匹配。全部 PDF、SQLite、来源目录及网页 JSON 均通过 git check-ignore 检查。
- 渲染检查最新南北方图所在 PDF 页面、第 52 周日期笔误页面、官网补正的第 2 周页面。
- 默认桌面视口的浏览器检查通过：页面加载、两张图、年度开关、全取消后的空状态、恢复选择、数据点官方来源链接、年份筛选和纠错后的第 2 周数值；无控制台错误。CSV 下载事件监听超时，但实际文件已生成，直接核验下载的 2025 年 CSV 全部 52 行数值及官方链接与 JSON 一致。未进行移动端浏览器检查。
- 实际发布包只有 6 个允许文件，未包含 PDF、SQLite 或任何本机路径；注入私有文件的离线用例也确认不会复制。
- 既有周六 07:10 计划保持不变，LaunchAgent 仍为空闲、runs=0；未执行真实 `main.py` 或触发任务。美团与日报仓库工作区/提交未改动。

### 边界与未执行项

- 为完成官方来源与修复错存/缺期，进行了公开目录/详情查询和两份 PDF 下载；没有写入坚果云、调用 LLM、发送消息或启动云端任务。
- GitHub 远程仓库和公开 Pages 尚未建立，当前交付为本地数据库、可预览页面及可发布文件包。
- 没有批量补抓 2023 年及更早报告；没有与美团项目合并或估计相关性。未来联合分析需明确地域映射、时间聚合和数据版本。

## 2026-08-31 04:59:39 Asia/Shanghai — 核对缺周与后续 ILI 引用差异

- 写入者模型：未知（运行环境未暴露）。
- 设备：TianhaodeMacBook-Pro.local（macOS 26.6.2，arm64）。

### 阶段总结

- 新增离线审计工具 `flu_data.audit_revisions`：只读查询主库、校验每份活动 PDF 的 SHA-256，按版面抽取前一周与往年同期 ILI% 引用。合并摘要/正文重复表述，跨年按周历处理，历史年份严格依原文保留；缺少当期报告或重复值冲突不纳入可配对统计。
- 核对 138 期、276 条当周观测，2024-W01 至 2026-W34 无内部缺周；首个图示年度前段缺 2023-W14 至 W52 共 39 周，未来未收录期不计入该范围缺失。
- 共提取 1,104 条引用，其中 512 条有原值可配对，241 条不同，涉及 139 个地区×目标周。前一周差异为 81/274，差异项平均绝对差约 0.15、最大 0.9 个百分点；历史同期为 160/238，差异项平均绝对差约 0.33、最大 1.4 个百分点。其余 592 条引用因缺少早年原报告未比较。
- 单列 2025-W01 南北方历史年份标签异常，以及 2024-W08 北方、2024-W32 南方比较措辞与显示数值不符；不猜测或改写。记录全部来源页码/原文/哈希及同一目标周的引用时间序列，不把文字差异自动认定为正式修订。
- 新增本地 HTML/Markdown 报告生成器，支持地区、引用类型、目标年份、差异阈值和周次筛选。全部结果保存在忽略目录 `data/revision_audit/`；报告只含文本与官网链接，不嵌入 PDF，不加入现有站点发布清单。

### 已执行验证

- 30 项 Python 单元测试全部通过（新增 10 项审计测试），覆盖年份列表/区间、跨年含第53周、异常年份保留、拒绝歧义和不完整引用、重复段落去重与冲突排除、当周值校验、去重统计、缺周与只读数据库保护。
- 4 项 JavaScript 图表测试、Python compileall、现有站点及审计内联 JavaScript 语法检查、git diff --check 通过。
- pypdf layout 模式提取的全部 1,104 条引用与另一提取引擎 Poppler pdftotext 的结果逐条一致；全部引用均有摘要与正文两个一致来源，没有未完成抽取或重复值冲突。
- 渲染并检查最大前一周差异（2024-W06/07 南方）、最大历史差异（2024-W28 与 2026-W28 北方）、南方最大历史差异（2024-W12 与 2026-W12）及 2025-W01 年份标签异常，共 7 个完整 PDF 页面。
- 浏览器核验摘要和差异表显示、原文展开、南北方/类型组合筛选、周次搜索与键盘清空、阈值与年份筛选；默认 241 条、≥0.5 个百分点 54 条、全部可配对 512 条，2026 年目标观测 66 条，与 JSON 一致。无控制台错误或桌面整页横向溢出；未进行移动端浏览器检查。
- 对生成 HTML 独立解析确认 512 行、默认 241 行可见，全部链接为官方来源，无 PDF 嵌入或外部脚本。主 SQLite 与现有网页数据的 SHA-256 在审计前后保持一致。

### 边界与未执行项

- 初次独立提取验证因 pdftotext 不在 PATH 中未运行，随后定位 bundled Poppler 并完成全量复核。浏览器空字符串填充未清除搜索框，随后用实际键盘操作清空并验证全部筛选结果；无需修改页面逻辑。
- 未下载更多历史 PDF，未修改坚果云、主库原值、已有图表、定时任务、美团或日报项目；未创建远程仓库、上传审计产物或发布站点。


## 2026-08-31 05:21:18 Asia/Shanghai — 按目标周保存五类报告来源值

- 写入者模型：未知（运行环境未暴露）。
- 设备：TianhaodeMacBook-Pro.local（macOS 26.6.2，arm64）。

### 阶段总结

- 按用户给出的 24/7、24/8、25/7、26/7、27/7 例子，以“年/周”解释并实现当期、下一周、后一年、后两年、后三年的来源分类。底层新增 `report_references` 保存每个来源 PDF 版本的引用值、目标年/周、南北方、页码、原文与措辞；`reference_extractions` 保存提取版本、导入时间和异常诊断。
- 新增 `observation_sources` 明细视图及 `weekly_source_values` 五列视图。按来源独立保存，相同数字不去重，缺值为 SQL NULL；无当期原报告的早年引用也保存，当期列为空。超出三年窗口的原文引用保留为 `outside_window`，不猜测调整年份；重复段落冲突候选及证据保留，冲突数值不进入五列表。
- 将引用提取接入既有离线数据库重建事务和增量缓存；初次对原库补入引用而不重提原值，之后新报告自动补充相应来源。来源 PDF 内容变更时保留旧版本引用；引用无法完整解析会回滚整批数据并保留旧网页导出。
- 正式库新增 1,104 条引用，连同原有 276 条当期观测，共有 1,380 条来源记录。五列表有 588 个“地区×目标周”，其中 312 个缺当期原报告；2 条超出三年窗口的引用仅保留在来源明细。正式库 138 期原报告及 276 条当周值不变。
- 网站继续仅从 `current_observations` 导出当期值，未更改页面、曲线口径或公开 JSON 结构；多来源值不进入发布文件。补充来源分类、SQL 查询、缺值/异常处理、增量更新和审计文件快照说明。

### 已执行验证

- 38 项 Python 测试、4 项 JavaScript 图表测试全部通过；新增 8 项覆盖重复数字独立保留、未来来源空值及后续填充、无原报告引用隔离、跨年/第 53 周、异常年份、PDF 版本保留、整批回滚、引用冲突与原库补入。Python compileall、JavaScript 语法检查、git diff --check 通过。
- 迁移前在忽略的 tmp 目录备份 SQLite、网页 JSON 和全部本地 PDF 哈希。先用 138 份真实归档从零构建临时库，再对正式库迁移；1,104 条引用的数值、目标、来源哈希、措辞、页码和原文均与此前审计结果逐条相同。
- 正式库原有 reports、report_versions、observations、report_flags 四张表的所有记录与迁移前完全一致。新增表和视图与临时全量构建一致，SQLite integrity_check、foreign_key_check、每个目标/地区/来源唯一性检查通过。
- 全部归档 PDF 字节未变；网页 JSON 除 generated_at 外全部字段一致；重新只读运行审计得到相同覆盖、全部引用、差异统计、异常与引用时间序列。再次增量构建 changed=0、references_changed=0。
- 实例核对 2024-W07：南方当期/下一周/后一年/后两年/后三年为 7.6/7.5/7.7/7.4/NULL；北方为 5.7/5.5/5.4/5.5/NULL。缺少 2027-W07 来源保持空值。
- 数据库、网页数据、审计文件和迁移备份均命中 Git 忽略规则，没有生成数据进入暂存范围。未改页面代码，因此不重复浏览器视觉测试，以完整导出内容对比和图表测试验证网页口径不变。

### 边界

- 全程离线，未下载报告、修改坚果云、运行真实 main.py、触发定时任务或修改计划；未改美团/日报仓库，未创建远程仓库或发布站点。
- 审计 HTML/Markdown/JSON 保持原有快照，数据库引用随正常重建更新；要查看之后新增数据的差异统计仍需显式运行审计命令。
